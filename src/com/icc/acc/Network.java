package com.icc.acc;

/**
 * Simple enum to hold the current operators ICC will support.
 * 
 * @author Rob Powell
 * @version 1.0
 */
public enum Network {
	O2, METEOR, VODAFONE, TESCO, EMOBILE, THREE
}